management:
 endpoints:
  web:
    exposure:
      include: '*'
# Enabling H2 Console
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:file:~/test
    username: sa
    password:
  h2:
    console:
      path: /h2-console
      enabled: true

